@extends('layouts.admin')

@section('page_title', 'Chỉnh Sửa Sản Phẩm')
@section('header_icon', 'fas fa-edit')

@section('content')
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-lg-8" style="background: white; padding: 30px; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
            <h1 class="h3 mb-4"><i class="fas fa-edit me-2"></i> Sửa Sản Phẩm</h1>

            @if($errors->any())
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <strong><i class="fas fa-exclamation-triangle me-2"></i>Lỗi Xác Thực:</strong>
                    <ul class="mb-0 mt-2">
                        @foreach($errors->all() as $error)
                            <li>{{ $error }}</li>
                        @endforeach
                    </ul>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            @endif

            <div class="card border-0 shadow-lg">
                <div class="card-header" style="background: linear-gradient(135deg, #8B3A3A 0%, #A85252 100%); color: white; border-radius: 8px 8px 0 0; border: none;">
                    <h5 class="mb-0">{{ $product->name }}</h5>
                </div>
                <div class="card-body p-4">
                    <form method="POST" action="{{ route('admin.products.update', $product->id) }}" enctype="multipart/form-data">
                        @csrf
                        @method('PUT')
                        
                        <!-- Tên Sản Phẩm -->
                        <div class="mb-4">
                            <label for="name" class="form-label fw-bold">Tên Sản Phẩm <span class="text-danger">*</span></label>
                            <input 
                                type="text" 
                                id="name"
                                name="name" 
                                class="form-control form-control-lg @error('name') is-invalid @enderror" 
                                placeholder="Nhập tên sản phẩm" 
                                value="{{ $product->name }}"
                                required />
                            @error('name')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror
                        </div>
                        
                        <!-- Danh Mục & Giá -->
                        <div class="row g-3 mb-4">
                            <div class="col-md-6">
                                <label for="category_id" class="form-label fw-bold">Danh Mục <span class="text-danger">*</span></label>
                                <select 
                                    id="category_id"
                                    name="category_id" 
                                    class="form-select form-select-lg @error('category_id') is-invalid @enderror" 
                                    required>
                                    <option value="">-- Chọn danh mục --</option>
                                    @foreach($categories as $c)
                                        <option value="{{ $c->id }}" {{ $product->category_id == $c->id ? 'selected' : '' }}>{{ $c->name }}</option>
                                    @endforeach
                                </select>
                                @error('category_id')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror
                            </div>
                            <div class="col-md-6">
                                <label for="price" class="form-label fw-bold">Giá (VND) <span class="text-danger">*</span></label>
                                <input 
                                    type="number" 
                                    id="price"
                                    name="price" 
                                    class="form-control form-control-lg @error('price') is-invalid @enderror" 
                                    placeholder="0" 
                                    min="0" 
                                    step="1000" 
                                    value="{{ $product->price }}"
                                    required />
                                @error('price')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror
                            </div>
                        </div>
                        
                        <!-- Số Lượng Tồn Kho -->
                        <div class="mb-4">
                            <label for="quantity" class="form-label fw-bold">Số Lượng Tồn Kho <span class="text-danger">*</span></label>
                            <input 
                                type="number" 
                                id="quantity"
                                name="quantity" 
                                class="form-control form-control-lg @error('quantity') is-invalid @enderror" 
                                placeholder="0" 
                                min="0" 
                                value="{{ $product->quantity }}"
                                required />
                            @error('quantity')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror
                        </div>
                        
                        <!-- Mô Tả Sản Phẩm -->
                        <div class="mb-4">
                            <label for="description" class="form-label fw-bold">Mô Tả Sản Phẩm</label>
                            <textarea 
                                id="description"
                                name="description" 
                                class="form-control @error('description') is-invalid @enderror" 
                                rows="5" 
                                placeholder="Nhập mô tả chi tiết về sản phẩm">{{ $product->description }}</textarea>
                            @error('description')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror
                        </div>
                        
                        <!-- Upload Ảnh Sản Phẩm -->
                        <div class="mb-4">
                            <label for="image" class="form-label fw-bold">Hình Ảnh Sản Phẩm</label>
                            
                            @if($product->image)
                                <div class="mb-3">
                                    <p class="text-muted small mb-2"><i class="fas fa-image me-1"></i>Ảnh hiện tại:</p>
                                    <div style="width: 200px; height: 200px; background: #f5f1e8; border-radius: 8px; display: flex; align-items: center; justify-content: center; overflow: hidden; border: 1px solid #e0e0e0;">
                                        <img src="/storage/{{ $product->image }}" style="max-width: 100%; max-height: 100%; object-fit: contain;" alt="{{ $product->name }}" />
                                    </div>
                                </div>
                            @endif
                            <div class="input-group mb-2">
                                <input 
                                    type="file" 
                                    id="image"
                                    name="image" 
                                    class="form-control form-control-lg @error('image') is-invalid @enderror" 
                                    accept="image/*"
                                    onchange="previewImage(event)" />
                            </div>
                            <small class="text-muted d-block mt-2">Chọn ảnh mới (JPG, PNG - tối đa 2MB), bỏ qua nếu không cần thay đổi</small>
                            @error('image')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror

                            <label class="form-label fw-bold mt-3">Ảnh Gallery</label>
                            <div class="mb-2 d-flex gap-2 flex-wrap">
                                @foreach($product->images as $img)
                                    <div style="position: relative; width:120px;">
                                        <img src="/storage/{{ $img->path }}" style="width:100%; height:100px; object-fit:cover; border-radius:6px;" />
                                        <label style="position:absolute; top:6px; right:6px;">
                                            <input type="checkbox" name="remove_images[]" value="{{ $img->id }}" />
                                        </label>
                                    </div>
                                @endforeach
                            </div>

                            <input type="file" id="images" name="images[]" class="form-control mb-2" accept="image/*" multiple />

                            <!-- New Image Preview -->
                            <div id="imagePreview" class="mt-3" style="display: none;">
                                <p class="text-muted small mb-2"><i class="fas fa-image me-1"></i>Ảnh mới:</p>
                                <img id="previewImg" src="" style="max-width: 200px; border-radius: 8px; box-shadow: 0 4px 8px rgba(0,0,0,0.1);" alt="Preview" />
                            </div>

                        <!-- Variants Editor -->
                        <div class="mb-4 mt-4">
                            <label class="form-label fw-bold">Variants</label>
                            <table class="table table-sm" id="variantsTable">
                                <thead>
                                    <tr>
                                        <th>SKU</th>
                                        <th>Option</th>
                                        <th>Price</th>
                                        <th>Quantity</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach($product->variants as $variant)
                                    <tr>
                                        <td><input type="text" class="form-control form-control-sm variant-sku" value="{{ $variant->sku }}"></td>
                                        <td><input type="text" class="form-control form-control-sm variant-option" value="{{ $variant->option }}"></td>
                                        <td><input type="number" step="0.01" class="form-control form-control-sm variant-price" value="{{ $variant->price }}"></td>
                                        <td><input type="number" class="form-control form-control-sm variant-qty" value="{{ $variant->quantity }}"></td>
                                        <td><button type="button" class="btn btn-danger btn-sm removeVariant">×</button></td>
                                    </tr>
                                    @endforeach
                                </tbody>
                            </table>
                            <button type="button" class="btn btn-outline-primary btn-sm" id="addVariantRow">Thêm Variant</button>
                            <small class="text-muted d-block mt-2">Nhấn "Thêm Variant" để thêm dòng; form sẽ chuyển thành textarea trước khi gửi.</small>
                            <textarea id="variants" name="variants" class="d-none">{{ old('variants') }}</textarea>
                        </div>
                        </div>

                        <hr class="my-4">

                        <!-- Action Buttons -->
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="{{ route('admin.products.index') }}" class="btn btn-secondary btn-lg">
                                <i class="fas fa-times me-2"></i> Hủy
                            </a>
                            <button type="submit" class="btn btn-dark btn-lg">
                                <i class="fas fa-save me-2"></i> Cập Nhật Sản Phẩm
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
function previewImage(event) {
    const file = event.target.files[0];
    const preview = document.getElementById('imagePreview');
    const previewImg = document.getElementById('previewImg');
    
    if (file) {
        const reader = new FileReader();
        reader.onload = function(e) {
            previewImg.src = e.target.result;
            preview.style.display = 'block';
        };
        reader.readAsDataURL(file);
    } else {
        preview.style.display = 'none';
    }
}
</script>
@endsection
